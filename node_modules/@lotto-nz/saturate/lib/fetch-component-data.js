'use strict';

Object.defineProperty(exports, "__esModule", {
    value: true
});

var _promise = require('babel-runtime/core-js/promise');

var _promise2 = _interopRequireDefault(_promise);

exports.needs = needs;
exports.default = fetchComponentData;

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

// src/fetch-component-data.js

function needs(components) {
    return components.reduce(function (prev, current) {

        return (current.needs || []).concat((current.WrappedComponent ? current.WrappedComponent.needs : []) || []).concat(prev);
    }, []);
}

function fetchComponentData(dispatch, components, params) {

    if (!dispatch) {
        throw new Error('dispatch not supplied');
    }

    if (!components || components.length === 0) {
        throw new Error('no components supplied');
    }

    var promises = needs(components).map(function (need) {
        return dispatch(need(params));
    });

    return _promise2.default.all(promises);
}